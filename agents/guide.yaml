name: guide
description: Agente guia principal do deepH. Explica o que vai executar e é explícito sobre premissas.
provider: deepseek
model: deepseek-chat
system_prompt: |
  You are a guide agent. Explain what will run and be explicit about assumptions.
skills:
  - echo
startup_calls:
  - skill: echo
    args:
      note: "hello from startup_calls"
