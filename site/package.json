{
  "name": "deeph-site",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20 <24"
  },
  "scripts": {
    "clean": "node -e \"const fs=require('fs'); for (const p of ['.next','.next-dev','tsconfig.tsbuildinfo']) fs.rmSync(p,{ recursive:true, force:true });\"",
    "doctor:node": "node -e \"const major=Number(process.versions.node.split('.')[0]); const allow=process.env.DEEPH_ALLOW_UNSUPPORTED_NODE==='1'; if (major >= 24 && !allow) { console.error('\\n[deeph-site] Node 24 detectado e bloqueado para dev (instabilidade de webpack cache/chunks). Use Node 20 LTS.\\n[deeph-site] Se quiser forcar mesmo assim: DEEPH_ALLOW_UNSUPPORTED_NODE=1 npm run dev\\n'); process.exit(1); } if (major >= 24 && allow) { console.warn('\\n[deeph-site] Rodando em Node 24 por override. Se houver erro de chunk/cache, volte para Node 20.\\n'); }\"",
    "predev": "npm run doctor:node && npm run clean",
    "prebuild": "npm run clean",
    "dev": "next dev",
    "dev:unsafe": "DEEPH_ALLOW_UNSUPPORTED_NODE=1 next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@radix-ui/react-dialog": "^1.1.15",
    "@radix-ui/react-label": "^2.1.8",
    "@radix-ui/react-scroll-area": "^1.2.10",
    "@radix-ui/react-select": "^2.2.6",
    "@radix-ui/react-slot": "^1.2.4",
    "@radix-ui/react-tabs": "^1.1.13",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "framer-motion": "^12.34.3",
    "lucide-react": "^0.575.0",
    "next": "15.5.12",
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "react-markdown": "^10.1.0",
    "tailwind-merge": "^3.5.0",
    "tailwindcss-animate": "^1.0.7"
  },
  "devDependencies": {
    "@types/node": "22.13.5",
    "@types/react": "19.0.10",
    "@types/react-dom": "19.0.4",
    "autoprefixer": "^10.4.27",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.19",
    "typescript": "5.8.2"
  }
}
